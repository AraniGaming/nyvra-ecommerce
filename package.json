{
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && CI=true npm run build --prefix client",
    "start": "npm start --prefix server",
    "seed": "npm run seed --prefix server",
    "create-admin": "npm run create-admin --prefix server"
  },
  "dependencies": {
    "cors": "^2.8.6"
  }
}
